Requirements:
cd backend
pip install flask
pip install flask-cors


To run website:
cd backend
python app.py
(ctrl+c to close)

cd frontend
npm run dev
click on http://localhost:5173/
(ctrl+c to close)

On website:
Enter instagram login. Make sure authentication app 2FA is enabled. 
SMS 2FA is not supported and will not work.